CREATE TABLE [IF NOT EXISTS] restaurants (
   id datatype(length) column_contraint,
   name datatype(length) column_contraint,
   location datatype(length) column_contraint,
   rating
   image
);

id:String
        name:String
        description:String
        location:String
        rating:Float
        image:String